export interface Credentials {
  config: {
    apiKey: string;
    authDomain: string;
    storageBucket: string;
    databaseURL: string;
    projectId: string;
    messagingSenderId: string;
  };
}
